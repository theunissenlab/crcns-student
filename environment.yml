name: crcns-python
channels:
  - conda-forge
dependencies:
  # Core scientific python
  - python=3
  - numpy
  - matplotlib
  - pandas
  - pyqt5
  - scipy
  - scikit-image
  - scikit-learn


  # Jupyter Environment
  - notebook
  - ipython

  - pip:
      # Neuro stuff
      - git+https://github.com/choldgraf/mne-spikes@master

      # Utilities
      - download
      - nbgrader
      - tqdm

      # Auditory analysis
      - git+https://github.com/theunissenlab/soundsig
      - librosa
